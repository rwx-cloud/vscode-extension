{
  "name": "mint-language-server",
  "description": "Language server for Mint workflow files",
  "version": "0.1.0",
  "main": "./out/server.js",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "vscode-languageserver": "^8.1.0",
    "vscode-languageserver-textdocument": "^1.0.8"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "typescript": "^5.0.0"
  }
}
